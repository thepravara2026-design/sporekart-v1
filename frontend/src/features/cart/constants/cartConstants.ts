/**
 * Cart domain constants. Mirrors the backend `cart.max-item-quantity` default.
 */
export const CART_MAX_ITEM_QUANTITY = 50;

/** Minimum quantity for a cart line (backend `UpdateCartItemCommand` `@Min(1)`). */
export const CART_MIN_ITEM_QUANTITY = 1;
